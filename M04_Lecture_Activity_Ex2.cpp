/* Program name: M03_Lecture_Activity.cpp
* Author: Ty Stratton
* Date last updated: 11/11/2024
* Purpose: Create while, for, do-while loops for 5 soccer players
*/


#include <iostream>
#include <fstream>
#include <string>
#include <iomanip>

using namespace std;


/*
exercise 1 while loop
int main()
{
    int i = 0;
    int count = 1;
    double score, total, average;
    while (i < 5){
        cout << "What's the score of the soccer player " << count + i << ": ";
        cin >> score;
        total = score + total;
        i++;
    }
    average = total / 5; 
    cout << "The average score for the five soccer players is " << average;
    return 0;
}
exercise 1 for loop
int main()
{
    double score, total, average;
    int count = 1;
    for (int i = 0; i < 5; i++) {
        cin >> score;
        total = score + total;
    }
    average = total / 5; 
    cout << "The average score for the five soccer players is " << average;
    return 0;
}
exercise 1 do while
int main()
{
    double score, total, average;
    int i = 0;
    int count = 1;
    do {
        cout << "What's the score of the soccer player " << count + i << ": ";
        cin >> score;
        total = score + total;
        i++;
    } while (i < 5);
    average = total / 5; 
    cout << "The average score for the five soccer players is " << average;
    return 0;
}
*/

